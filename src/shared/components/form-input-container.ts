import { createMain } from '../../utils/create-elements/create-tags';
import { FORM_INPUT } from '../styles';

export const container = createMain({classes: FORM_INPUT.container, parent: document.body});
